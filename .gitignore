
# Python bytecode (orchestrator)
__pycache__/
*.pyc

# 敏感内容永不提交：个人隐私 / API key / env / 代理订阅与节点
# （用户名 jianlongliu 本身可以出现，见 ~/.ante/skills/writing-docs）
.env
.env.*
*.env
token
*.token
*.key
*.pem
*credential*
*secret*
*subscription*
mihomo*.yaml

# 备份副本一律只在本地、不进仓库。2026-09-21 起本机备份统一搬进独立目录
# ~/.local/state/backups/（结构镜像 $HOME，如 ~/.local/state/backups/.config/niri/layout.kdl.bak-20260921-bgcolor），
# 所以仓库树里本不该再出现备份；下面这条是防手滑的兜底（理由与清单见 docs/local-overrides.md §0.1）。
# 规则用 `*.bak*` 而不是 `*.bak-*`：后者覆盖不到不带时间戳的 `x.bak` 这类。
*.bak*
