# Font Configuration
# font-family = FiraCode Nerd Font Mono
font-family = SFMono Nerd Font
font-family = Microsoft YaHei
font-size = 9

# Window Configuration
window-decoration = false
window-padding-x = 12
window-padding-y = 12
background-opacity = 0.85
# niri 不实现 KDE blur 协议，ghostty 自带模糊在 niri 上无效且会双层模糊，磨砂交给 niri 的 background-effect
background-blur-radius = 0

# Cursor Configuration
cursor-style = block
cursor-style-blink = true

# Scrollback
scrollback-limit = 3023

# Terminal features
mouse-hide-while-typing = true
copy-on-select = false
confirm-close-surface = false

# Disable annoying copied to clipboard
app-notifications = no-clipboard-copy,no-config-reload

# Key bindings for common actions
# Omarchy 风格通用剪贴板：Super+C/V/X 由 ghostty 自己接管。
# 上游 Hyprland 版是把 Ctrl+Insert / Shift+Insert 注入终端（send_shortcut），
# niri 没有注入能力，所以改成「niri 不占键 + 终端自己绑」——两边都要成立才有效。
keybind = super+c=copy_to_clipboard
keybind = super+v=paste_from_clipboard
keybind = super+x=text:\x18
# 上游 ghostty 配置里的备用键位（Insert 系）
keybind = control+insert=copy_to_clipboard
keybind = shift+insert=paste_from_clipboard
#keybind = ctrl+c=copy_to_clipboard
#keybind = ctrl+v=paste_from_clipboard
keybind = ctrl+shift+n=new_window
keybind = ctrl+t=new_tab
keybind = ctrl+plus=increase_font_size:1
keybind = ctrl+minus=decrease_font_size:1
keybind = ctrl+zero=reset_font_size

# Material 3 UI elements
unfocused-split-opacity = 0.7
unfocused-split-fill = #44483d

# Tab configuration
gtk-titlebar = false

# Shell integration
shell-integration = detect
shell-integration-features = cursor,sudo,title,no-cursor
keybind = shift+enter=text:\n

# Rando stuff
gtk-single-instance = true

# Dynamic theme colors (Omarchy writes this per theme)
config-file = ?"~/.local/state/omarchy/current/theme/ghostty.conf"
